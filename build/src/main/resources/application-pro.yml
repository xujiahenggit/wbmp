server:
  port: 8088
spring:
  datasource:
    # 如果存在多个数据源，监控的时候可以通过名字来区分开来
    name: oracle
    # 连接数据库的url
    url: jdbc:oracle:thin:@39.104.146.218:1521:ORCL
    # 连接数据库的账号
    username: QD_BANK
    #  连接数据库的密码
    password: 123456
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      # 扩展插件
      # 监控统计用的filter:stat 日志用的filter:log4j 防御sql注入的filter:wall
      filters: stat
      # 最大连接池数量
      max-active: 20
      # 初始化时建立物理连接的个数。初始化发生在显示调用init方法，或者第一次getConnection时
      initial-size: 1
      # 获取连接时最大等待时间，单位毫秒
      max-wait: 60000
      # 最小连接池数量
      min-idle: 1
      time-between-eviction-runs-millis: 60000
      # 连接保持空闲而不被驱逐的最长时间
      min-evictable-idle-time-millis: 300000
      # 用来检测连接是否有效的sql，要求是一个查询语句
      # 如果validationQuery为null，testOnBorrow、testOnReturn、testWhileIdle都不会其作用
      validation-query: select count(1) from dual
      # 申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunsMillis，执行validationQuery检测连接是否有效
      test-while-idle: true
      # 申请连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能
      test-on-borrow: false
      # 归还连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能
      test-on-return: false
      # 是否缓存preparedStatement，即PSCache
      pool-prepared-statements: false
      # 要启用PSCache，必须配置大于0，当大于0时，poolPreparedStatements自动触发修改为true
      max-open-prepared-statements: -1
mybatis-plus:
  #如果将来整个项目启动较慢，可把此处扫描包位置写到每个依赖的具体dao层，缺点为每添加一个新模块，就需要在此处重新添加，优点为项目启动速度快
  type-aliases-package: com.bank
  mapper-locations: classpath*:/mybatis-mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  config: classpath:log4j2.yml


#自定义属性
#日志版本
LOG:
  VERSION: 1
# FTP 地址
DAT:
  FTP_IP: 172.16.1.89
  #FTP 端口号
  FTP_PORT: 21
  #FTP 用户名
  FTP_USER_NAME: newfrt
  #FTP 密码
  FTP_PASS_WORD: newfrt
  #FTP 文件路径
  FTP_FILE_PATH: /nhr/
  #大数据文件 本地服务器路径
  LOCALPATH: D:/bankdata/
  #大数据 机构文件名称
  ORGFILE_NAME: NHR_ORGANIZATION_?.dat
  #大数据 用户文件名称
  USERFILE_NAME: NHR_EMPLOYEE_?.dat
#巡查证明文件上传路径
PARTORAL:
  PROVE_FILE_PATH: /prove
  PROVE_ACCESS_PATH: /prove
##################素材上传配置################
#素材临时存储路径-共享
MATERIAL:
  FILE_PATH_FILE: /vedio
  #http素材临时路径-共享
  HTTP_PATH: http://39.104.146.218:8080
  #素材临时存储FTP_IP
  FILE_FTP_IP: 39.105.86.51
  #素材临时存储FTP_PORT
  FILE_FTP_PORT: 21
  #素材临时存储FTP_USER
  FILE_FTP_USER: yusys
  #素材临时存储FTP_PASS
  FILE_FTP_PASS: abcd1234
  #素材临时存储路径-FTP
  FILE_FTP_PATH: /material
  #http素材临时路径-FTP
  FILE_FTP_HTTP_PATH: http://39.105.86.51:8080/material

tomcat:
  baseIp: http://39.104.146.218:8080 #应用服务器地址
  contextPath: /bkserver
  port: 8080 #tomcat启动端口